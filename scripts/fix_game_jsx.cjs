const fs = require('fs'); let c = fs.readFileSync('src/pages/Game.jsx', 'utf-8'); c = c.replace(/const fetchQuizSet = async \(\) => \{/, 'const fetchQuizSet = async () => {\n    setLoading(true);'); c = c.replace(/setQuestions\(qData \|\| \[\]\);\n      \}/, 'setQuestions(qData || []);\n      }\n      setLoading(false);'); c = c.replace(/const handleStart = async \(\) => \{/, 'const handleStart = async () => {\n    if (questions.length === 0) {\n      setErrorMsg(\'등록된 문제가 없습니다. 관리자에게 문의하세요.\');\n      return;\n    }'); fs.writeFileSync('src/pages/Game.jsx', c);
